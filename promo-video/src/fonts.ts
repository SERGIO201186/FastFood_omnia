// Google Fonts require network access, which is not reliably available in
// every render environment. Use a bundled system sans-serif stack instead,
// matching the app's own `'Inter',ui-sans-serif,system-ui,sans-serif` look.
export const fontFamily =
  "'Liberation Sans', 'DejaVu Sans', ui-sans-serif, system-ui, sans-serif";
